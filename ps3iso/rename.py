from pathlib import Path


def rename_iso(iso_path, rename_extras=True):
    iso_path = Path(iso_path)


def rename_all(path, **kwargs):
    path = Path(path)

    for p in []:
        rename_iso(p, **kwargs)
